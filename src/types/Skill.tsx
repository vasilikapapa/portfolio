// Defines the structure of a Skill object

export interface Skill {
    title: string;               //Category name (e.g., Frontend. Backend)
    items: string[];             //List of specific skills/tools
}